package model.board;

import java.util.ArrayList;

public class HexBoard {

	public static final int DEFAULT_SIZE = 30;
	public static final int DEFAULT_CENTER_POINT = 0;
	
	public HexBoard(){
		ArrayList<Hex> board = new ArrayList<Hex>(10);
		//TODO: create a testing algorithm that will create and then draw hexagons
//		for (int i = 0; i < 2; i++) {
//			board.add(new Hex(new HexPosition(`, y, z, hexCenter)))
//		}
	}
	
}
